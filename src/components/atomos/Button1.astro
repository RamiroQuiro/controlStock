---
const { onClick, id, style, type,className } = Astro.props;

---

<button
  id={id}
  onclick={onClick}
  type={type}
  class={`${className}  px-2 py-1 rounded-3xl font-  lowercase md:tracking-normal tracking-tighter active:ring-2 border-primary-texto/20 hover:ring-2 hover:ring-primary-100/50 duration-200 text-xs  border bg-gray-100 hover:bg-white hover:text-primary-textoTitle`}
>
  <slot />
</button>
