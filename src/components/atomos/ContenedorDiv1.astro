---
const { style } = Astro.props as { style: string };
---

<div
  class={`${style} flex w-full md:p-5 px-2 py-3 space-y-5 min-h-[60vh] flex-col items-start justify-start relative`}
>
  <slot />
</div>
