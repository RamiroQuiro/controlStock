---

const { title, h1 } = Astro.props;

const {user}=Astro.locals
---

<main
id="mainDashboard"
  data-usisisi={user?.id}
  class="top-12 md:right-0 z-0  w-full mt-5 md:16  print:hidden flex flex-col items-center justify-center"
>
 <div class="w-full flex items-center justify-start sha gap-4">
   {
     h1 && (
       <div class="md:w-1/3 w-full gap-3 mb-3 md:ml-6 ml-2 justify-normal flex items-center ">
         {/* <ArrowLeftCircle/> */}
        <h1 class="text-xl  font-semibold text-primary-textoTitle  capitalize">{h1}</h1>
      </div>
    )

  }
  <slot name="menu"/>
  </div>
  <div
    class="w-full py flex flex-col md:px-6 px-2 gap-2  overflow-y-auto items-center justify- rounded-lg h-[85vh] pb-20 md:pb-10"
  >
    <slot />
  </div>
</main>
