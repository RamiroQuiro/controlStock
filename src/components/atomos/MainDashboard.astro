---
import { ArrowLeftCircle, Backpack } from "lucide-react";

const { title, dataIds, h1 } = Astro.props;
---

<main
  class="top-12 md:right-0 z-0  w-full mt-5 md:16  print:hidden flex flex-col items-center justify-center"
>
 <div class="w-full flex items-center justify-start sha gap-4">
   {
     h1 && (
       <div class="w-1/3 gap-3 mb-3 ml-6 justify-normal flex items-center ">
         {/* <ArrowLeftCircle/> */}
        <h1 class="text-xl  font-semibold text-primary-textoTitle ">{h1}</h1>
      </div>
    )

  }
  <slot name="menu"/>
  </div>
  <div
    class="w-full py flex flex-col px-6 gap-2  overflow-y-auto items-center justify- rounded-lg h-[85vh] pb-10"
  >
    <slot />
  </div>
</main>
