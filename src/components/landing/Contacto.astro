---
import Button1 from '../atomos/Button1.astro';
---

<section id="contacto" class="py-16">
  <div class="container mx-auto px-6 md:px-8">
    <div class="max-w-4xl mx-auto text-center mb-12">
      <h2 class="text-3xl md:text-4xl font-bold text-primary-textoTitle mb-4">
        Contáctanos
      </h2>
      <p class="text-xl text-primary-texto max-w-2xl mx-auto">
        ¿Tienes preguntas o necesitas más información? Estamos aquí para
        ayudarte.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-8">
      <div class="bg-white p-8 rounded-lg shadow-md">
        <form class="space-y-4">
          <div>
            <label for="name" class="block text-primary-textoTitle mb-2"
              >Nombre</label
            >
            <input
              type="text"
              id="name"
              name="name"
              required
              class="w-full px-4 py-2 border border-primary-150 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300"
            />
          </div>
          <div>
            <label for="email" class="block text-primary-textoTitle mb-2"
              >Correo Electrónico</label
            >
            <input
              type="email"
              id="email"
              name="email"
              required
              class="w-full px-4 py-2 border border-primary-150 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300"
            />
          </div>
          <div>
            <label for="message" class="block text-primary-textoTitle mb-2"
              >Mensaje</label
            >
            <textarea
              id="message"
              name="message"
              rows="4"
              required
              class="w-full px-4 py-2 border border-primary-150 rounded-md focus:outline-none focus:ring-2 focus:ring-primary-300"
            ></textarea>
          </div>
          <Button1
            style="w-full bg-primary-600 text-white hover:bg-primary-500"
          >
            Enviar Mensaje
          </Button1>
        </form>
      </div>

      <div
        class="bg-white p-8 rounded-lg shadow-md flex flex-col justify-between"
      >
        <div>
          <h3 class="text-2xl font-semibold text-primary-textoTitle mb-4">
            Información de Contacto
          </h3>
          <div class="space-y-4">
            <div>
              <p class="font-medium text-primary-textoTitle">
                Correo Electrónico
              </p>
              <p class="text-primary-texto">rama.exe.13@gmail.com</p>
            </div>
            <div>
              <p class="font-medium text-primary-textoTitle">Teléfono</p>
              <p class="text-primary-texto">+54 (385) 5815662</p>
            </div>
            <div>
              <p class="font-medium text-primary-textoTitle">Dirección</p>
              <p class="text-primary-texto">Santiago del Estero, Argentina</p>
            </div>
          </div>
        </div>
        <div class="mt-6 text-primary-texto">
          <p>Horario de Atención:</p>
          <p>Lunes a Viernes: 9:00 AM - 6:00 PM</p>
        </div>
      </div>
    </div>
  </div>
</section>
