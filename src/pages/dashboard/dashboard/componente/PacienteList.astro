---
import { Activity, Clock } from "lucide-react";
import formatDate from "../../../../utils/formatDate";
import extraerHora from "../../../../utils/extraerHora";
const {ultimasAtenciones}=Astro.props


---


<div class=" rounded-lg w-full">

    <div class="flex flex-col gap-2 items-center justify-normal w-full">
      {ultimasAtenciones?.map((aten) => (
        <a href={`/dashboard/consultas/${aten.id}`} class="py-1 px-4 w-full rounded-lg hover:bg-white  hover:shadow-md hover:-translate-y-0.5 duration-200 bg-primary-bg-componentes -colors cursor-pointer">
          <div class="flex justify-between items-center">
            <div>
              <h3 class="font-medium text-gray-800">{aten.nombre}{' '}{aten.apellido}</h3>
              <div class="flex items-center gap-2 mt-1 text-sm text-gray-500">
                <Clock class="w-4 h-4" />
                <span class="pr-2">Última visita: {formatDate(aten.inicioAtencion)}{extraerHora(aten.inicioAtencion)}</span>
              </div>
            </div>
            <div class="flex items-center gap-2">
              <span class={`text-sm capitalize`}>{aten.obraSocial}</span>
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>