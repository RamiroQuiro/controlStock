---
import FormularioCargaProducto from './FormularioCargaProducto.tsx';

const { user} = Astro.locals;
const{depositos,ubicaciones}= Astro.props;
console.log('depositos',depositos)
console.log('ubicaciones',ubicaciones)

---

<!-- 2. Renderizar la isla de React, pasándole los datos como props -->
<FormularioCargaProducto 
    client:load 
    depositos={depositos} 
    ubicaciones={ubicaciones} 
    userId={user.id}
    empresaId={user.empresaId}
/>
