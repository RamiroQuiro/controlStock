<div class="download-loader-container fixed top-4 right-4 w-64 h-1/3 p-4  rounded-lg bg-white shadow-lg z-50 border border-gray-200">
  <div class="flex items-center justify-center h-full gap-3">
    <div class="download-arrow">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M12 3v13"/>
        <path d="m9 13 3 3 3-3"/>
        <path d="M20 21H4"/>
      </svg>
    </div>
    <div class="flex-1">
      <p class="text-sm font-medium text-gray-700">Descargando PDF</p>
      <p class="text-xs text-gray-500">Por favor espere...</p>
    </div>
  </div>
</div>

<style>
  .download-loader-container {
    display: none;
    animation: slideIn 0.3s ease;
  }

  .download-arrow {
    color: #5FA5FA;
    animation: downloadBounce 1.5s ease infinite;
  }

  @keyframes slideIn {
    from {
      transform: translateY(-100%);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes downloadBounce {
    0%, 100% {
      transform: translateY(0);
      opacity: 1;
    }
    50% {
      transform: translateY(6px);
      opacity: 0.7;
    }
  }
</style> 